# Vanilla deck manager (reference)

An earlier deck manager written in plain HTML, CSS and JavaScript, kept here as a
reference for behavior and UI while building the Next.js app.

- Reference only: it is not built, linted, styled by Tailwind or deployed.
- Do not import from it or edit it to fix issues; port ideas into `app/` and `lib/` instead.
